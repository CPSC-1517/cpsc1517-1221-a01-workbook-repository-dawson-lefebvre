@page
@model RazorPagesDemo.Pages.RollDiceModel
@{
}
<div class="text-center">
    <h1>Six-Sided Dice Roll Simulator</h1>
    <img src="@Model.DiceFaceValueImage"
         alt="Dice Image"
         width="200px"
         height="200px" />

    @if (Model.InfoMessage != null)
    {
        <div>
            <p>@Model.InfoMessage</p>
        </div>
    }

    <form method="post">

        <div>
            <label for="BetAmount">Bet Amount</label>
            <input type="number" min="1" max="20" id="BetAmount" name="BetAmount" value="@Model.BetAmount">
        </div>

        <div>
            <label for="SelectedDiceSide" class="form-text">Bet Number</label>
            <select class="form-select" id="SelectedDiceSide" name="SelectedDiceSide">
                <option selected>Open this select menu</option>
                <option value="1">One</option>
                <option value="2">Two</option>
                <option value="3">Three</option>
                <option value="4">Four</option>
                <option value="5">Five</option>
                <option value="6">Six</option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Roll Dice</button>
    </form>
</div>
